#!/bin/bash
# Bootstrap configuration.
#   $ git clone https://github.com/kwarrick/dotfiles config
#   $ ./config/bootstrap
cd ~
ln -s dots/vimrc ~/.vimrc
ln -s dots/vim ~/.vim
ln -s dots/tmux.conf ~/.tmux.conf
mkdir ~/.vim/bundle
git clone https://github.com/gmarik/vundle ~/.vim/bundle/vundle

